# タイムカレンダー PWA セットアップ手順

このアプリはすでに「スマホアプリ化」の対応を済ませています。残りは以下の3ステップで、
すべて無料・所要20〜30分程度です。アカウント作成やパスワード入力はご自身の手で行ってください
（Claudeが代行できない工程です）。

- ステップ1: GitHub Pagesで公開する（URLを固定で持つ）
- ステップ2: Firebaseでクラウド同期を設定する（Mac⇄iPhone自動同期）
- ステップ3: iPhoneのホーム画面に追加する（アプリのように使う）

---

## ステップ1: GitHub Pagesに公開する

1. https://github.com にアクセスし、アカウントがなければ作成してログイン
2. 右上の「+」→「New repository」
   - Repository name: `timecalendar`（好きな名前でOK）
   - 「Public」を選択
   - 「Create repository」をクリック
3. 「uploading an existing file」または「Add file」→「Upload files」をクリック
4. Finderで `タイムカレンダーApp` フォルダを開き、**中身をすべて**（`index.html` / `manifest.json` /
   `sw.js` / `firebase-config.js` / `icons` フォルダ）ドラッグ&ドロップ
   - フォルダそのものではなく「中身」をアップロードしてください（`icons` フォルダはそのままドラッグでOK）
5. 下の「Commit changes」をクリック
6. リポジトリ画面の「Settings」タブ →左メニュー「Pages」
   - 「Source」を「Deploy from a branch」
   - 「Branch」を `main` / `(root)` にして「Save」
7. 1〜2分待つと、ページ上部に公開URLが表示されます
   （例: `https://あなたのユーザー名.github.io/timecalendar/`）
   - **これが今後Mac・iPhoneどちらからも開くURLです**

---

## ステップ2: Firebaseでクラウド同期を設定する

1. https://console.firebase.google.com を開き、Googleアカウントでログイン
2. 「プロジェクトを作成」→ プロジェクト名は任意（例: `timecalendar`）
   → Googleアナリティクスは「無効」でOK → 「プロジェクトを作成」
3. 左メニュー「構築」→「Authentication」→「始める」
   - 「Sign-in method」タブ →「メール/パスワード」を選択 → 有効にして「保存」
4. 左メニュー「構築」→「Firestore Database」→「データベースの作成」
   - ロケーション: `asia-northeast1`（東京）を推奨
   - 「本番環境モードで開始」を選択
5. 「ルール」タブを開き、内容を以下に置き換えて「公開」

   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /users/{uid}/appdata/{docId} {
         allow read, write: if request.auth != null && request.auth.uid == uid;
       }
     }
   }
   ```

6. 画面左上の歯車アイコン →「プロジェクトの設定」→ 下にスクロールして「マイアプリ」欄の
   `</>`（ウェブ）アイコンをクリック
   - アプリのニックネーム: 任意（例: `timecalendar-web`）→「アプリを登録」
   - 表示された `firebaseConfig = { ... }` の中身をコピー
7. GitHubのリポジトリ画面で `firebase-config.js` を開き、鉛筆マーク（Edit）をクリックして、
   コピーした値を貼り付けます。例:

   ```js
   export const firebaseConfig = {
     apiKey: "AIzaSy...",
     authDomain: "timecalendar-xxxx.firebaseapp.com",
     projectId: "timecalendar-xxxx",
     storageBucket: "timecalendar-xxxx.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456789:web:abcdef123456"
   };
   ```

8. 「Commit changes」で保存 →1分ほどでGitHub Pagesに反映されます

---

## ステップ3: 使い始める

1. Mac・iPhoneどちらでもよいので、ブラウザでステップ1の公開URLを開く
2. サイドバー上部の「同期設定」をタップ → メールアドレスと、ご自身で決めた
   パスワード（6文字以上）を入力して「新規登録」
   - これで1台目の端末が使えるようになります
3. もう一方の端末で同じURLを開き、「同期設定」→ **同じ**メールアドレス・パスワードで「ログイン」
   - これでMacとiPhoneのデータが自動的にリアルタイム同期されます

### iPhoneでアプリのように使う（ホーム画面に追加）

1. iPhoneのSafariで公開URLを開く
2. 下部（またはアドレスバー横）の共有アイコン（□に↑）をタップ
3. 「ホーム画面に追加」→「追加」
4. ホーム画面にアイコンが追加され、タップするとブラウザのアドレスバーなしで
   全画面アプリとして開きます

---

## 知っておいてほしいこと

- **費用**: GitHub Pages・Firebase（Sparkプラン）とも無料枠の範囲内で収まります。
- **データ容量**: Firestore無料枠は1ドキュメントあたり約1MB。メモを多めに書いても数年分は問題なく入ります。
- **同時編集**: 2台で同時刻に別々の内容を保存すると、後から保存した方が優先されます
  （基本1人で使う想定なので通常は気にしなくて大丈夫です）。
- **オフライン**: ホーム画面に追加したアプリは電波がなくても閲覧・入力でき、
  電波が戻ると自動的にクラウドへ反映されます。
- **アプリの見た目やカテゴリを後から変更したい場合**: `index.html` を編集して、GitHubに
  再アップロード（Commit changes）すれば、両端末とも自動的に最新版になります。

## うまくいかないとき

- サイドバーに「同期エラー」と出る → `firebase-config.js` に貼り付けた値を再確認
- ログインできない → 新規登録済みなら「ログイン」、初めてなら「新規登録」を押しているか確認
- ホーム画面のアイコンが更新されない → 一度Safariでアプリを閉じてから開き直す
  （Service Workerが数分以内に新しいバージョンへ自動更新します）
